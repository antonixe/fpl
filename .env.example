# FPL API base URL (public, exposed to browser)
NEXT_PUBLIC_FPL_API_BASE=https://fantasy.premierleague.com/api

# App version (set automatically by CI/Vercel, or manually)
NEXT_PUBLIC_APP_VERSION=1.0.0

# Public site URL for sitemap/OG tags (set to your deployed domain)
NEXT_PUBLIC_SITE_URL=https://fpl.example.com

# Node environment (set by platform — do not change)
# NODE_ENV=production

# --- Vercel Deployment (set as GitHub Actions secrets) ---
# VERCEL_TOKEN=<your-vercel-token>
# VERCEL_ORG_ID=<your-vercel-org-id>
# VERCEL_PROJECT_ID=<your-vercel-project-id>
